{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:base"],
  "packageRules": [
    {
      "description": "Next.js and TypeScript upgrades are coordinated manually to ensure compatibility across the stack.",
      "matchPackageNames": ["next", "typescript"],
      "enabled": false
    }
  ]
}
